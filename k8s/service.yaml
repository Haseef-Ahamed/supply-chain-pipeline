apiVersion: v1
kind: Service
metadata:
  name: hsf_app-service
  namespace: supply-chain-demo
  labels:
    app: hsf_app
spec:
  type: NodePort
  selector:
    app: hsf_app
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080
