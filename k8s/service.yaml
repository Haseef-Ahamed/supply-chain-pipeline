apiVersion: v1
kind: Service
metadata:
  name: hsf-app-service
  namespace: supply-chain-demo
  labels:
    app: hsf-app
spec:
  type: NodePort
  selector:
    app: hsf-app
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080
