apiVersion: v1
kind: Service
metadata:
  name: myapp-service
  namespace: supply-chain-demo
  labels:
    app: myapp
spec:
  type: NodePort
  selector:
    app: myapp
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080
